# AUTO-GENERATED — do not edit directly.
# Source:  theholocron/holocron · packages/astromech/src/thin-callers.ts
# Tool:    holocron sync
# Changes: edit source in theholocron/holocron
name: Verification

on: # yamllint disable-line rule:truthy
  push:
    branches: [main, alpha]
  pull_request:

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  statuses: write

jobs:
  test:
    name: Test
    uses: theholocron/.github/.github/workflows/verification.unitTests.yml@main
    with:
      run-unit: true
    secrets: inherit
