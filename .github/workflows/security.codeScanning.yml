# AUTO-GENERATED — do not edit directly.
# Source:  theholocron/holocron · packages/astromech/src/thin-callers.ts
# Tool:    holocron sync
# Changes: edit source in theholocron/holocron
name: Security

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 0 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  security:
    name: Code Scanning
    uses: theholocron/.github/.github/workflows/security.codeScanning.yml@main
    secrets: inherit
